image: docker:latest

services:
  - docker:dind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_TLS_CERTDIR: ""

stages:
  - test

test-docker:
  stage: test
  script:
    - docker info
    - docker run hello-world
